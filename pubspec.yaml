name: dart_jieba
description: Pure Dart 中文分词 — Python jieba port with binary trie and delta-gzip compression.
version: 0.1.0
repository: https://github.com/marsup-space/dart-jieba
license: MIT

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
