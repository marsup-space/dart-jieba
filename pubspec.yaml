name: dart_jieba
description: 大结巴 — Pure Dart 中文分词，Python jieba 完整移植，binary trie + delta-gzip.
version: 1.0.0
repository: https://github.com/marsup-space/dart-jieba
license: MIT

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
